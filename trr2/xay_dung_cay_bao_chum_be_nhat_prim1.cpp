#include<bits/stdc++.h>
#define MAX 100
using namespace std;
class graph{
    int dau, cuoi, c;
}edge;
class graph{
    int n, s, C[MAX][MAX], truoc[MAX], dH, ne, neT, T[MAX][MAX];
    edge G[MAX], Tree[MAX];
    public:
    void readdata();
    void innit(){
        for(int i = 1; i <= n; i++) chuaxet[i] + 1;
    }
    dfstree(int u);
    chuaxet[u] = 0;
    

};

